/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // HAPUS experimental.serverActions dan swcMinify
};

export default nextConfig;
